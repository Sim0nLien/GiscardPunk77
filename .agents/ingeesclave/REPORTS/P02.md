# P02 — Fondation dégâts, santé et mort idempotente

- Lot : `P02`
- Statut : `complete`
- Début : `2026-08-14`
- Dernière mise à jour : `2026-08-14`
- Demande source : implémenter le contrat de dégâts, santé, mort idempotente, hitbox, cible capsule de sandbox et tests Edit Mode.

## Objectif et périmètre

- Objectif : donner à l'arme, aux futurs gardes et aux civils un chemin unique et testable pour infliger ou recevoir des dégâts.
- Inclus : `DamageInfo`, `IDamageable`, `Health`, événements de dommage/mort, `DamageableHitbox`, outil Editor idempotent et tests Edit Mode ciblés.
- Exclu : armure, membres, multiplicateurs de hitbox, ragdoll, factions avancées, arme hitscan, prefab et modification de scène.

## Tâches

### T01 — Ouvrir le lot et borner la cible de sandbox

- Statut : `complete`
- Action réalisée : périmètre P02 vérifié contre le devis, le contrat et les assemblages P01 ; rapport P02 créé.
- Fichiers concernés : `.agents/ingeesclave/WORK_STATE.md`, `.agents/ingeesclave/REPORTS/P02.md`, `.agents/ingeesclave/REPORTS/INDEX.md`, `.agents/ingeesclave/FEEDBACK.md`.
- Preuve ou validation : P01 est `complete` après le retour utilisateur « 5 test bon » ; le contrat réserve la création de `NpcSandbox.unity` au lot P04.
- Résultat observable : P02 apparaît comme lot actif, sans toucher à `SampleScene.unity` ni créer prématurément le sandbox.

### T02 — Construire le chemin unique de dégâts

- Statut : `complete`
- Action réalisée : ajout de `DamageInfo` immutable, du contrat `IDamageable`, de `Health` et de `DamageableHitbox` dans Gameplay/Combat.
- Fichiers concernés : `DamageCategory.cs`, `DamageInfo.cs`, `IDamageable.cs`, `Health.cs` et `DamageableHitbox.cs`.
- Preuve ou validation : revue statique : les propriétés de `DamageInfo` n'ont aucun setter, `Health` est le seul détenteur des champs de santé et la hitbox ne contient ni calcul ni multiplicateur.
- Résultat observable : tout futur consommateur transmet un `DamageInfo` à `TryApplyDamage`; `Health` borne les valeurs, émet `Damaged`, puis `Died` une seule fois par cycle de vie.

### T03 — Préparer l'authoring et les tests

- Statut : `complete`
- Action réalisée : ajout de l'outil idempotent de capsule et de sept tests Edit Mode dans une assembly Gameplay dédiée.
- Fichiers concernés : `DamageableTestTargetAuthoring.cs`, `GiscardPunk77.Gameplay.Tests.asmdef` et `HealthTests.cs`.
- Preuve ou validation : JSON asmdef valide et résultat Unity du 2026-08-14 : 7/7 tests Gameplay verts ; la suite complète Core + Gameplay affiche 12/12.
- Résultat observable : après P04, le menu `Tools > GiscardPunk77 > Create or Update Damageable Test Target` crée ou met à jour une unique capsule dans `NpcSandbox`; avant P04, le menu est désactivé pour préserver les scènes.

### T04 — Rendre le lot compréhensible sans prérequis

- Statut : `complete`
- Action réalisée : création d'un guide visible expliquant le problème, les mots utiles, le chemin des dégâts, l'exemple 34+34+34, la méthode de conception et les limites.
- Fichiers concernés : `Docs/INGEESCLAVE/INDEX.md` et `Docs/INGEESCLAVE/P02.md` ; règles et générateur ajoutés au skill personnel Ingéesclave.
- Preuve ou validation : le générateur a créé l'index et P02, puis une seconde exécution a retourné `Created : False`; le validateur de mémoire est `Valid` et le validateur officiel skill-creator répond `Skill is valid!`.
- Résultat observable : une personne débutante peut suivre P02 depuis `Docs/INGEESCLAVE/P02.md` sans ouvrir les dossiers cachés ni connaître les noms des classes à l'avance.

## Validations

- Validé automatiquement : revue statique des contrats, absence de setter sur `DamageInfo`, absence de consommateur externe qui calcule la santé, assembly de tests JSON valide et sept méthodes `[Test]` détectées.
- Validé automatiquement : Unity a compilé Gameplay et Gameplay.Tests ; `TestResults.xml` indique 7/7 tests P02 réussis et 12/12 sur la suite Edit Mode complète.

## Comment voir le changement

1. Ouvrir d'abord `Docs/INGEESCLAVE/P02.md` pour comprendre le chemin de dégâts avant de lire le code.
2. Dans Unity, revenir sur le projet et attendre la fin de l'import ; la Console ne doit afficher aucune erreur C#.
3. Ouvrir `Window > General > Test Runner`, choisir `EditMode`, puis lancer `GiscardPunk77.Gameplay.Tests`.
4. Après le lot P04 seulement, ouvrir `Assets/_Project/Scenes/Tests/NpcSandbox.unity` puis choisir `Tools > GiscardPunk77 > Create or Update Damageable Test Target` deux fois.

## Résultat attendu

- Les sept tests P02 sont verts. Les dégâts `34 + 34 + 34` font passer une santé de 100 à 0 et l'événement `Died` est reçu une fois. Après P04, une seule capsule `Damageable Test Target` équipée de `Health` et `DamageableHitbox` est sélectionnée, même après deux exécutions du menu.

## Limites et non-vérifié

- `NpcSandbox.unity` n'existe pas encore : la démonstration visuelle de la capsule sera réalisée par le banc P03, tandis que P04 conservera la responsabilité du NavMesh.

## Feedback lié

- `FB-004` — demande de réalisation P02, implémentation livrée ; validation Unity en attente.
- `FB-005` — demande de guides pédagogiques visibles, traitée.

## Suite

- Prochaine action exacte : P02 est clôturé ; suivre le rapport P03 sans modifier les contrats de santé validés.
