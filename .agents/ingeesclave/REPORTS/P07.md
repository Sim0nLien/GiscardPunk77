# P07 — Signature du joueur et perception visuelle

- Lot : `P07`
- Statut : `complete`
- Début : `2026-08-15`
- Dernière mise à jour : `2026-08-15`
- Demande source : produire une observation visuelle structurée depuis `IVisibilityTarget`, avec posture, exposition, occultation, échantillonnage réparti, gizmos et protocole Play Mode.

## Objectif et périmètre

- Objectif : mesurer ce qu'un PNJ peut voir sans modifier directement l'état d'un garde.
- Inclus : score distance/angle/posture, accumulation d'exposition, raycast d'occultation, observation immutable, capteur configurable, gizmos, banc P07, tests et documentation.
- Exclu : awareness/suspicion P08, état de garde, luminosité, camouflage, vision périphérique complexe, identification sociale et intégration dans `SampleScene`.

## Tâches

### T01 — Enregistrer P06 et auditer la frontière de visibilité

- Statut : `complete`
- Action réalisée : le retour « Tous les tests passent » ferme le défaut automatisé P06 ; `IVisibilityTarget` et `PlayerController` ont été relus sans toucher au mouvement.
- Fichiers concernés : mémoire Ingéesclave, `IVisibilityTarget.cs`, `PlayerController.cs`.
- Preuve : `FB-013`; `PlayerController` expose toujours son booléen `IsCrouching` et la position de caméra via Core.
- Résultat observable : aucun fichier P07 runtime ne nomme ni ne référence `PlayerController`.

### T02 — Isoler le calcul de vision

- Statut : `complete`
- Action réalisée : ajout de paramètres bornés, d'un score géométrique et de fonctions pures pour exposition, progression et phase d'échantillonnage.
- Fichiers concernés : `NpcVisionParameters.cs`, `NpcVisionScore.cs`, `NpcVisionEvaluation.cs`.
- Preuve : harness exécuté avec debout `1,00`, accroupi `0,34`, dos hors cône et occultation à zéro ; sept tests Edit Mode compilent.
- Résultat observable : à position égale, accroupi réduit portée, coefficient de gain et vitesse de détection.

### T03 — Produire une observation structurée

- Statut : `complete`
- Action réalisée : ajout de `NpcVisionObservation` immutable et de `NpcVisionSensor`, qui raycaste puis publie `ObservationUpdated` sans commander un état métier.
- Fichiers concernés : `NpcVisionObservation.cs`, `NpcVisionSensor.cs`.
- Preuve : assembly AI compilée sans erreur ; audit sans `Guard`, awareness ni suspicion.
- Résultat observable : `LastObservation` décrit cible, distance, angle, scores, posture, occultation, exposition, progression et détection.

### T04 — Répartir l'échantillonnage et dessiner les gizmos

- Statut : `complete`
- Action réalisée : chaque capteur reçoit un décalage initial hashé dans son intervalle ; le capteur dessine un cône circulaire et mémorise seulement son dernier rayon.
- Fichiers concernés : `NpcVisionSensor.cs` et `NpcVisionEvaluation.cs`.
- Preuve : test déterministe/borné de phase compilé ; aucune recherche d'objet dans `Update`.
- Résultat observable : cône cyan, rayon vert si clair, rouge jusqu'au blocage avec segment cyan vers la cible, gris hors cône ; la sphère de cible grossit avec l'exposition et devient verte à détection.

### T05 — Fournir le banc P07 et le protocole H3

- Statut : `complete`
- Action réalisée : ajout d'une cible pilotable par Inspector et d'un outil Editor idempotent qui ne crée/met à jour que `P07 Vision Test Rig` dans le sandbox existant.
- Fichiers concernés : `NpcVisionSandboxTarget.cs`, `NpcVisionSandboxAuthoring.cs`, guide P07.
- Preuve : compilation `Assembly-CSharp-Editor` sans erreur ; la scène n'est modifiée qu'à l'exécution explicite du menu.
- Résultat observable : cible à 8 m, marqueur proche à 5 m, position derrière, mur déplaçable et diagnostics runtime du capteur.

## Validations

- Validé automatiquement : compilations `GiscardPunk77.AI`, `GiscardPunk77.AI.Tests` et `Assembly-CSharp-Editor`, 0 erreur et 0 avertissement.
- Validé automatiquement : harness pur des cas centraux exécuté avec succès.
- Validé statiquement : dépendances runtime inchangées `Core <- Gameplay <- AI`, aucune dépendance vers le contrôleur joueur concret et aucune logique P08.
- Non vérifié dans Unity : sept nouveaux tests Edit Mode, création de `P07 Vision Test Rig`, raycast physique et ressenti des valeurs H3.
- Validé dans Unity par l'utilisateur le 2026-08-15 : « Les tests passent ».

## Réglages initiaux du banc

| Réglage | Valeur |
|---|---:|
| Portée debout | 12 m |
| Angle total du cône | 100° |
| Exposition minimale debout | 0,6 s avant modulation géométrique |
| Portée accroupie | 65 % (7,8 m) |
| Exposition requise accroupie | 175 % (1,05 s) |
| Coefficient de gain accroupi | 65 % |
| Fréquence | 8 échantillons/s, avec phase répartie |

## Limites

- P07 n'interprète pas l'observation en suspicion ou alerte ; cette responsabilité appartient à P08.
- Une rupture de vue remet l'exposition continue à zéro ; la mémoire de suspicion appartient à P08.
- Le capteur observe une cible configurée et ne recherche pas automatiquement tous les acteurs.
- `SampleScene` n'est pas modifiée dans ce lot.

## Comment voir le changement

1. Lancer l'assembly Edit Mode `GiscardPunk77.AI.Tests` ; les 11 tests AI doivent être verts, dont les 7 `NpcVisionEvaluationTests`.
2. Exécuter `Tools > GiscardPunk77 > P07 > Create or Update Vision Sandbox`.
3. Ouvrir `Assets/_Project/Scenes/Tests/NpcSandbox.unity`, déplier `P07 Vision Test Rig`, cadrer la racine avec F, sélectionner `P07 Observer` et activer `Gizmos` dans la vue Scene.
4. Passer en Play Mode et suivre les quatre scénarios du guide `Docs/INGEESCLAVE/P07.md`.

## Résultat attendu

- Face/debout : rayon vert, sphère d'observation qui grossit puis devient verte.
- Face/accroupi à 5 m : progression sensiblement plus lente après reset ; à 8 m, cible hors portée accroupie.
- Derrière : ligne grise, hors cône, aucune détection.
- Mur centré : rayon rouge jusqu'au mur, segment cyan vers la cible, exposition à zéro et aucune détection.

## Signe d'échec

- Un test rouge, une référence AI à `PlayerController`, une détection derrière ou au travers du mur, une cible accroupie détectée aussi vite dans les mêmes conditions, ou des rayons tous synchronisés bloque H3.

## Feedback lié

- `FB-013` — validation des tests P06 et demande P07.
- `FB-014` — validation utilisateur des tests P07 et ouverture de P08.

## Suite

- Prochaine action exacte : conserver les réglages de vision pour le réglage combiné H3 après P08.
