# P08 — Suspicion, mémoire courte et indicateur

- Lot : `P08`
- Statut : `complete`
- Début : `2026-08-15`
- Dernière mise à jour : `2026-08-15`
- Demande source : ajouter une awareness séparée de la vision et un indicateur de présentation qui suit la caméra.

## Objectif et périmètre

- Objectif : transformer les observations de P07 en suspicion lisible, sans changer la logique de vision ni créer de comportement de garde.
- Inclus : configuration partagée, suspicion normalisée, hystérésis, mémoire de dernière vision, indicateur, banc isolé, tests et guide.
- Exclu : investigation P11, alerte globale P09, combat, luminosité, Behavior, animation finale et modification de `SampleScene`.

## Tâches

### T01 — Enregistrer la validation P07 et cadrer P08

- Statut : `complete`
- Action réalisée : validation utilisateur P07 enregistrée ; P07 est clos et H3 reste une validation conjointe, car les distances de vision et la vitesse de suspicion se règlent ensemble.
- Preuve : `FB-014` et mémoire actualisée.

### T02 — Créer la configuration et le modèle d'hystérésis

- Statut : `complete`
- Action réalisée : ajout de `NpcAwarenessConfig`, d'un tuning immutable et de `NpcAwarenessModel`, avec entrées/sorties distinctes pour les deux seuils d'hystérésis.
- Fichiers concernés : `NpcAwarenessConfig.cs`, `NpcAwarenessModel.cs`, `Assets/_Project/Config/AI/NpcAwarenessConfig.asset`.
- Preuve : compilation AI/tests ; harness pur des transitions et de la décroissance.
- Résultat observable : la suspicion est bornée entre 0 et 1 et ne rebondit pas immédiatement au voisinage d'un seuil.

### T03 — Implémenter l'awareness et la mémoire

- Statut : `complete`
- Action réalisée : `NpcAwareness` s'abonne aux observations de `NpcVisionSensor`, avance avec un temps explicitement injectable, mémorise uniquement une ligne de vue claire et publie les changements réels d'état.
- Fichiers concernés : `NpcAwareness.cs`, `NpcAwarenessTests.cs`.
- Preuve : cinq tests Edit Mode compilés, dont perte de vue, reset et acquisition unique de l'alerte.
- Résultat observable : après la perte de vue, `LastSeenPosition` et `LastSeenTime` conservent la dernière valeur valide pendant que la suspicion décroît.

### T04 — Implémenter l'indicateur et le banc P08

- Statut : `complete`
- Action réalisée : `NpcAwarenessIndicator` écoute seulement `StateChanged`, pilote des objets visuels existants et s'oriente vers une caméra configurée. L'outil P08 génère un observateur, une cible, une caméra, un mur et les deux signaux sous une racine isolée.
- Fichiers concernés : `NpcAwarenessIndicator.cs`, `NpcAwarenessSandboxAuthoring.cs`.
- Preuve : compilation Editor sans erreur ; l'outil ne s'exécute que depuis son menu explicite.
- Résultat observable : calme caché, signal de suspicion facultatif puis `!` à l'alerte ; aucun objet `!` n'est instancié à chaque frame.

## Réglages initiaux partagés

| Réglage | Valeur |
|---|---:|
| Gain de suspicion | 0,9/s × progression de vision |
| Décroissance hors vue | 0,2/s |
| Entrée / sortie suspicion | 0,25 / 0,12 |
| Entrée / sortie alerte | 0,85 / 0,60 |
| Signal de suspicion | activé |
| Hauteur / échelle indicateur | 2,25 m / 0,7 |

## Validations

- Validé automatiquement : compilations `GiscardPunk77.AI`, `GiscardPunk77.AI.Tests` et `Assembly-CSharp-Editor`, 0 erreur et 0 avertissement.
- Validé automatiquement : harness pur P08 — hystérésis Suspicious/Alerted et décroissance bornée.
- Validé statiquement : `NpcAwareness` ne contient ni raycast, ni `Physics`, ni présentation ; `NpcAwarenessIndicator` ne contient ni détection, ni calcul de suspicion.
- Validé par l'utilisateur après livraison : « Les tests passent ». Le réglage visuel H3 n'a pas été détaillé, mais son suivi reste historique et ne bloque plus P09 explicitement demandé.

## Limites

- P08 ne déplace pas un PNJ vers la dernière position ; cette investigation appartient à P11.
- P08 ne propage aucune alerte entre PNJ ; cette responsabilité appartient à P09.
- `SampleScene` n'est pas modifiée.

## Comment voir le changement

1. Lancer `GiscardPunk77.AI.Tests` en Edit Mode : 16 tests AI attendus, dont les 5 `NpcAwarenessTests`.
2. Exécuter `Tools > GiscardPunk77 > P08 > Create or Update Awareness Sandbox`.
3. Ouvrir `Assets/_Project/Scenes/Tests/NpcSandbox.unity`, sélectionner `P08 Awareness Test Rig`, appuyer sur F, puis passer en Play Mode.
4. Observer l'Inspector de `P08 Observer` et la caméra `P08 Presentation Camera`; suivre le protocole du guide P08.

## Résultat attendu

- Le signal de suspicion apparaît une fois que la suspicion franchit 0,25 ; `!` remplace ce signal une fois à 0,85.
- Lors d'une perte de vue, dernière position/heure restent figées, la suspicion décroît et les sorties 0,60 puis 0,12 empêchent tout clignotement.
- `P08/Reset Awareness` efface la suspicion, la mémoire et l'indicateur.

## Signe d'échec

- Un test rouge, `!` répété sans nouvelle acquisition, état qui oscille autour d'un seuil, dernière position qui bouge hors vue, ou indicateur actif au calme bloque H3.

## Feedback lié

- `FB-014` — validation P07 et demande P08.

## Suite

- P08 est clos pour les tests confirmés. Le suivi qualitatif H3 peut être complété ultérieurement sans ouvrir un nouveau lot.
