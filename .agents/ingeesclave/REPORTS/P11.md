# P11 — Garde : repos, patrouille et investigation

- Lot : `P11`
- Statut : `waiting_human — porte H4`
- Début : `2026-08-20`
- Dernière mise à jour : `2026-08-20`
- Demande source : `FB-019` — première boucle non combattante complète du garde.

## Objectif et périmètre

- Objectif : fournir un garde capsule réutilisable qui alterne repos et patrouille, interrompt cette routine sur suspicion, inspecte une dernière position connue puis revient à son poste.
- Inclus : `GuardConfig`, `GuardContext`, points écrits, quatre actions fines, graphe Behavior, interruption d’alerte globale, couleurs temporaires, historique borné, prefab, tests et documentation.
- Exclu : combat, corps découverts, recherche multi-points, flanking, couvert dynamique, modification de `SampleScene` et assemblage E1+ de P13.

## Tâches

### T01 — Ouvrir P11 et corriger la reprise

- Statut : `complete`
- Action réalisée : demande `FB-019` enregistrée ; le devis canonique confirme que H4 couvre P10–P11, donc l’instruction explicite ouvre P11 malgré l’ancienne passation P10 trop restrictive.
- Fichiers concernés : mémoire Ingéesclave, contrat et présent rapport.
- Preuve : `NPC_DEVIS_DE_PROMPTS.md`, lignes P11 et tableau H4.
- Résultat observable : P11 dispose de son rapport et de son guide distincts.

### T02 — Séparer configuration, contexte et patrouille écrite

- Statut : `complete`
- Action réalisée : `GuardConfig` porte durées/couleurs ; `GuardPatrolRoute` porte trois offsets écrits relativement au poste capturé ; `GuardContext` relie le socle P10, conserve l’index de patrouille et huit transitions récentes.
- Fichiers concernés : `Assets/_Project/Scripts/AI/Behavior/Guard/`.
- Preuve : trois tests `GuardContextTests`, inspection du prefab et compilation Unity.
- Résultat observable : les points cyan restent ancrés au poste, tandis que l’historique apparaît dans l’Inspector de `GuardContext`.

### T03 — Créer les nodes fins et la réaction immédiate

- Statut : `complete`
- Action réalisée : un composite de routage délègue à `GuardIdleAction`, `GuardPatrolAction`, `GuardSuspiciousAction` et `GuardInvestigateLastKnownPositionAction`. Le mouvement passe exclusivement par `NpcMotor`.
- Fichiers concernés : `Assets/_Project/Scripts/AI/Behavior/Guard/Nodes/`.
- Preuve : compilation Roslyn AI sans erreur ; audit statique sans `NavMeshAgent`, raycast ou dégâts dans les nodes.
- Résultat observable : repos bleu, patrouille verte, orientation jaune, investigation/retour orange ; l’alerte globale annule le moteur et met le garde en rouge.

### T04 — Générer le graphe et le prefab capsule

- Statut : `complete`
- Action réalisée : l’outil Editor idempotent crée le graphe `P11 Guard Routine`, `GuardConfig.asset`, un matériau de debug et `Guard Capsule.prefab` sans toucher aux scènes.
- Fichiers concernés : `NpcBehaviorGraphAuthoring.cs`, `GuardPrefabAuthoring.cs`, assets P11 sous `Config/AI`, `Prefabs/AI` et `Art/Debug`.
- Preuve : journal `Logs/P11-authoring-interactive.log`, code de sortie Unity 0 ; YAML du graphe avec quatre enfants ordonnés et override prefab `Guard Context` valide.
- Résultat observable : le prefab et le graphe s’ouvrent directement depuis le Project ou les menus P11.

### T05 — Compiler et tester

- Statut : `complete`
- Action réalisée : compilation ciblée AI/tests/Editor, puis suite Unity Edit Mode complète. Un ancien harness P10 a été corrigé en initialisant explicitement `Health` avant son dégât létal.
- Fichiers concernés : `GuardContextTests.cs`, `NpcStateReaderTests.cs`.
- Preuve : `Logs/P11-tests-rerun.xml` — `28/28` passés, `0` échec ; Unity termine avec le code 0.
- Résultat observable : les tests couvrent points écrits, historique borné et interruption immédiate de l’alerte.

## Validations

- Validé automatiquement : assemblies AI, tests et Editor compilées sans erreur.
- Validé automatiquement : 28/28 tests `GiscardPunk77.AI.Tests` passent.
- Validé automatiquement : Unity a généré/importé le graphe, la configuration, le matériau et le prefab.
- Validé par inspection : aucun accès direct à `NavMeshAgent`, aucun raycast et aucun dégât dans les nodes Guard.
- À valider humainement H4 : graphe lisible en Debug, boucle de dix minutes sur NavMesh, suspicion puis retour au poste, couleur/historique et interruption globale visibles.

## Comment voir le changement

1. Ouvrir `Assets/_Project/Prefabs/AI/Guard Capsule.prefab` et `Assets/_Project/Config/AI/P11 Guard Routine.asset`.
2. Dans `NpcSandbox`, déposer le prefab sur le NavMesh puis assigner le même `AlertService` de scène à `NpcContext` et `NpcAlertReporter`, ainsi que la cible de vision à `NpcVisionSensor`.
3. Entrer en Play Mode, ouvrir le graphe et choisir l’instance dans **Debug**.
4. Suivre le protocole détaillé de `Docs/INGEESCLAVE/P11.md`, dont l’observation de dix minutes.

## Résultat attendu

- Le garde alterne bleu/vert entre poste et trois points sans destination invalide.
- Une suspicion interrompt la patrouille : jaune pendant l’orientation, orange pendant l’aller à la dernière position et le retour, puis bleu au poste.
- Une alerte globale annule immédiatement le trajet, colore la capsule en rouge et termine la routine non combattante en `Failure`, prête pour le futur branchement combat.
- `GuardContext.transitionHistory` ne conserve que les huit transitions les plus récentes.

## Limites et non-vérifié

- La boucle Play Mode de dix minutes n’a pas été observée automatiquement ; H4 reste ouverte.
- Le prefab ne peut pas sérialiser une référence de scène : `AlertService` et cible de vision sont volontairement à assigner après placement.
- Il n’existe encore ni combat P12, ni garde complet P13, ni intégration `SampleScene` P16.

## Feedback lié

- `FB-019` — demande P11, implémentée et en attente de validation visuelle H4.

## Suite

- Prochaine action exacte : suivre `Docs/INGEESCLAVE/P11.md` pendant dix minutes et rapporter si la boucle, la suspicion, le retour au poste, les couleurs, l’historique et l’alerte globale correspondent au résultat attendu.
