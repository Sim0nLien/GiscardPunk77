# P06 — API de porte et réservation de seuil

- Lot : `P06`
- Statut : `waiting_human`
- Début : `2026-08-14`
- Dernière mise à jour : `2026-08-15`
- Demande source : découpler `SlidingDoor` des PNJ, ajouter une réservation FIFO expirante et tester deux puis quatre capsules des deux côtés.

## Objectif et périmètre

- Objectif : faire traverser une porte étroite sans que plusieurs agents occupent le seuil en même temps.
- Inclus : contrat Gameplay commun, file FIFO, adaptation de `SlidingDoor`, orchestration AI `NpcDoorTraversal`, sandbox et tests.
- Exclu : verrouillage, droits, factions, Behavior nodes, perception et intégration dans `SampleScene`.

## Tâches

### T01 — Clôturer P05 et auditer la porte existante

- Statut : `complete`
- Action réalisée : validation utilisateur P05 enregistrée ; inspection des onze références `SlidingDoor` de `SampleScene` et conservation de son chemin d'interaction clavier/caméra.
- Fichiers concernés : `REPORTS/P05.md`, `FEEDBACK.md`, `Assets/Script/SlidingDoor.cs`.
- Preuve : `FB-010`; aucun changement n'est apporté au YAML de `SampleScene`.
- Résultat observable : P05 est `complete` et P06 est le seul lot actif.

### T02 — Créer le contrat commun et la file FIFO

- Statut : `complete`
- Action réalisée : ajout de `IDoorPassage`, d'un instantané immutable d'état et d'une file FIFO à propriétaire unique, rafraîchissement et expiration.
- Fichiers concernés : `Assets/_Project/Scripts/Gameplay/Doors/`.
- Preuve : compilation Gameplay ciblée réussie ; harness exécuté avec 20 passages FIFO et promotion après expiration.
- Résultat observable : `ActiveOwner` ne contient jamais plus d'un propriétaire et le suivant est promu lors d'un `Release` ou d'une expiration.

### T03 — Adapter SlidingDoor sans casser le joueur

- Statut : `complete`
- Action réalisée : `SlidingDoor` implémente le contrat, garde le raycast caméra et la touche E, expose deux points d'attente, publie les changements et anime encore son Rigidbody cinématique.
- Fichier concerné : `Assets/Script/SlidingDoor.cs`.
- Preuve : compilation ciblée `Assembly-CSharp` réussie ; le chemin `WasPressedThisFrame` appelle toujours l'alternance ouverture/fermeture.
- Résultat observable : un appel NPC demande seulement l'ouverture ; l'interaction joueur conserve son basculement manuel.

### T04 — Orchestrer une traversée AI libérable

- Statut : `complete`
- Action réalisée : ajout de `NpcDoorTraversal`, qui choisit le point proche, se met en file, approche, attend l'ouverture, rejoint l'autre côté et libère.
- Fichiers concernés : `NpcDoorTraversal.cs`, `NpcDoorTraversalStressProbe.cs`.
- Preuve : compilation AI ciblée réussie ; AI ne référence ni `SlidingDoor` ni `Guard`, et aucun `GetComponent` n'est exécuté dans `Update`.
- Résultat observable : états `WaitingForReservation`, `MovingToWaitingPoint`, `WaitingForPassableDoor`, `Traversing`, `Completed` ou `Failed`.

### T05 — Garantir la libération de cycle de vie

- Statut : `complete`
- Action réalisée : mort `Health.Died`, désactivation, destruction, reset et timeout appellent tous la même libération idempotente et annulent le moteur si nécessaire.
- Fichier concerné : `NpcDoorTraversal.cs`.
- Preuve : six scénarios Play Mode compilent, dont mort, disable, reset et timeout.
- Résultat observable : une capsule disparue ne peut pas conserver le seuil ; l'expiration de cinq secondes reste une deuxième sécurité côté porte.

### T06 — Construire le stress test visible

- Statut : `complete`
- Action réalisée : l'outil Editor P04/P06 configure une vraie porte coulissante fermée, quatre capsules alternées, un probe de vingt traversées et désactive l'ancien probe P04 pendant ce stress.
- Fichier concerné : `NpcSandboxNavigationAuthoring.cs`.
- Preuve : compilation ciblée `Assembly-CSharp-Editor` réussie ; outil limité à la racine générée du sandbox.
- Résultat observable : la Hierarchy contient `P06 Door Traversal Stress`; la Console termine par `P06 door stress completed 20 traversals.`.

### T07 — Ajouter les tests de réservation et capsules

- Statut : `complete`
- Action réalisée : quatre tests Edit Mode pour ordre/expiration/idempotence et six tests Play Mode pour deux/quatre capsules et libérations de cycle de vie.
- Fichiers concernés : `DoorReservationQueueTests.cs`, `NpcDoorTraversalPlayModeTests.cs`, asmdef Play Mode AI.
- Preuve : assemblies de tests compilées avec les références Unity ; harness FIFO réellement exécuté avec succès.
- Résultat observable : quatre nouveaux tests Gameplay et six nouveaux tests Play Mode apparaissent dans Test Runner.

### T08 — Corriger la congestion révélée par Unity

- Statut : `implemented`
- Action réalisée : le point d'attente opposé n'est plus utilisé comme destination finale dans le stress P06. Chaque capsule reçoit un dégagement A/B distinct ; `NpcDoorTraversal` conserve la réservation jusqu'à ce que ce dégagement soit atteint. Le test vérifie aussi l'ordre FIFO et décrit état, échec, position et destination de chaque capsule en cas de timeout.
- Fichiers concernés : `NpcDoorTraversal.cs`, `NpcSandboxNavigationAuthoring.cs`, `NpcDoorTraversalPlayModeTests.cs`.
- Preuve : les deux rapports utilisateur identifient `RecalculationLimitReached`; l'inspection source confirme que la première version terminait sur le point réutilisé comme approche par le propriétaire suivant. Les assemblies AI, tests Play Mode et outil Editor corrigés compilent avec Roslyn/Unity.
- Résultat observable : après régénération, `P06 Door Traversal Stress` contient quatre paires `P06 Clearance A/B`; une capsule ne rend son ticket qu'une fois arrivée à sa position latérale propre. La validation Unity du mouvement reste attendue.

## Validations

- Validé automatiquement : compilation Roslyn ciblée réussie pour `GiscardPunk77.Gameplay`, `GiscardPunk77.AI`, `Assembly-CSharp`, `Assembly-CSharp-Editor`, tests Gameplay et tests AI Play Mode.
- Validé automatiquement : harness pur réussi — `20 FIFO passages and abandoned-owner expiration`.
- Validé statiquement : aucune référence AI à `SlidingDoor`/`Guard`, aucun accès direct à `NavMeshAgent` depuis `NpcDoorTraversal`, aucun `GetComponent` dans ses boucles `Update`.
- Validé automatiquement après correction : compilation ciblée réussie pour `GiscardPunk77.AI`, `GiscardPunk77.AI.PlayMode.Tests` et `NpcSandboxNavigationAuthoring`; le contrôle d'ownership confirme que seul `NpcMotor` commande `NavMeshAgent`.
- Validé dans Unity par l'utilisateur : huit tests Play Mode sur dix passent, notamment les quatre libérations de cycle de vie et les quatre tests `NpcMotor`.
- Échec historique confirmé dans Unity : avant correction, les scénarios opposés à deux et quatre capsules ne terminaient pas ; les deux rapports reproduisent le même résultat `8/10`.
- Validé dans Unity par l'utilisateur le 2026-08-15 : « Tous les tests passent », ce qui inclut les deux scénarios opposés corrigés dans le lot de tests demandé.
- Non vérifié après correction : génération de scène, bake, stress visuel quatre capsules et interaction E.
- Limite d'environnement : la licence Unity CLI locale empêche d'exécuter automatiquement le Test Runner ; H2 doit être lancée dans l'éditeur licencié déjà utilisé par l'utilisateur.

## Limites

- P06 ne crée aucun droit d'accès et n'ajoute aucune connaissance de `Guard` dans la porte.
- Les portes ne possèdent ni verrou, ni droit avancé, ni priorité de faction.
- `SampleScene` n'est pas modifiée par P06 ; son interaction joueur doit être vérifiée en Play Mode.

## Comment voir le changement

1. Dans les tests Play Mode, relancer d'abord `TwoCapsulesFromOppositeSidesCompleteInOrder` et `FourCapsulesFromOppositeSidesAllComplete`.
2. Si les deux sont verts, lancer l'assembly complet `GiscardPunk77.AI.PlayMode.Tests`; s'arrêter et transmettre le nouveau diagnostic détaillé si un test reste rouge.
3. Après dix tests Play Mode verts, exécuter `Tools > GiscardPunk77 > P06 > Create or Update Door Stress Sandbox`.
4. Ouvrir `Assets/_Project/Scenes/Tests/NpcSandbox.unity`, sélectionner la racine P04 et cliquer `Bake` sur `NavMeshSurface`.
5. Activer `Gizmos`, passer en Play Mode et observer `P06 Door Traversal Stress` jusqu'à vingt passages.
6. Ouvrir ensuite `SampleScene` en Play Mode, viser une porte existante et appuyer sur E pour vérifier l'interaction joueur.

## Résultat attendu

- Une capsule traverse à la fois ; les trois autres restent en file, puis chaque capsule repart depuis son nouveau côté.
- Les deux scénarios opposés sont verts, terminent dans l'ordre FIFO et ne publient ni `RecalculationLimitReached` ni `MotorRejectedPath`.
- La Console affiche exactement `P06 door stress completed 20 traversals.` sans blocage permanent.
- La touche E ouvre et referme encore une porte de `SampleScene` lorsque le joueur la vise à portée.

## Signe d'échec

- Deux capsules occupent simultanément le seuil, le compteur cesse d'avancer, `NpcDoorTraversal failed` apparaît, une réservation survit à une capsule désactivée ou E ne commande plus la porte.

## Feedback lié

- `FB-010` — validation P05 et demande P06.
- `FB-011` — rapports Unity `8/10` et clarification du protocole H2.
- `FB-012` — reproduction textuelle des deux scénarios rouges.
- `FB-013` — validation de tous les tests après correction et ouverture explicite de P07.

## Suite

- Prochaine action exacte : conserver H2 visuel en suivi manuel ; le défaut automatisé est clos et P07 peut avancer sur sa branche indépendante.
